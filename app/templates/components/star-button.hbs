<a 
		href="#" 
		onclick={{action "click"}} 
		class="btn btn-info btn-lg  {{starred}}"
>
		{{#if @saved}}
				<span data-test="star-test"  class="glyphicon glyphicon-star"></span>Save
		{{else}}
				<span data-test="star-test"  class="glyphicon glyphicon-star-empty"></span>Save
		{{/if}}
</a>